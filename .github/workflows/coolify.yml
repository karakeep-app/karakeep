name: Build and Deploy to Coolify

on:
  push:
    branches:
      - main
    paths-ignore:
      # Ignore config-only changes - only build on actual code changes
      - 'docker/docker-compose*.yml'
      - '*.md'
      - 'docs/**'
      - '.github/**'
      - 'README*'
      - 'LICENSE'
      - '.gitignore'
      - '.env.sample'
  
  workflow_dispatch:  # Manual trigger for major updates

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/carsaig/karakeep  # YOUR image, not upstream

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        
      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Get latest release version
        id: version
        run: |
          # Get the latest release tag from GitHub
          LATEST_TAG=$(curl -s https://api.github.com/repos/karakeep-app/karakeep/releases/latest | jq -r .tag_name)
          echo "version=${LATEST_TAG}" >> $GITHUB_OUTPUT
          echo "Latest version: ${LATEST_TAG}"
          
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}
            type=raw,value=${{ steps.version.outputs.version }},enable={{is_default_branch}}
            type=raw,value=release,enable={{is_default_branch}}
            
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile
          target: aio
          platforms: linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            SERVER_VERSION=${{ steps.version.outputs.version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          
      - name: Summary
        run: |
          echo "## ✅ Build Complete - Coolify will auto-deploy" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** \`${{ env.IMAGE_NAME }}:latest\`" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** \`${{ steps.version.outputs.version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Platform:** linux/arm64 only" >> $GITHUB_STEP_SUMMARY
